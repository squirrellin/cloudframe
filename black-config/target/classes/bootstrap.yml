server:
  port: 4000

spring:
  application:
    name: config-client
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
      config:
        server-addr: 127.0.0.1:8848
        #从nacos读取文件格式
        file-extension: yaml
        group: PROD_GROUP
        #namespace: fa88cfad-fa05-44b3-b30a-a6f48ce8ed7c


#config文件格式 对应nacos: spring.applcation.name-active.file-extension